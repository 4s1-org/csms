{
  "name": "@4s1/css-lib",
  "version": "1.0.0",
  "private": true,
  "description": "Charging Station Simulator Library",
  "homepage": "https://gitlab.com/4s1/csms",
  "bugs": {
    "url": "https://gitlab.com/4s1/csms/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@gitlab.com/4s1/csms.git"
  },
  "license": "MIT",
  "author": "Steffen <steffen@4s1.de>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "prebuild": "rm -rf dist/",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "preinstall": "npx only-allow pnpm",
    "lbt": "npm run lint && npm run build && npm run test",
    "lint": "eslint --ext .ts src/",
    "lint:fix": "npm run lint -- --fix",
    "start": "node -r ts-node/register src/index.ts | pino-pretty",
    "start:prod": "node dist/index.js",
    "start:watch": "nodemon --watch src/ -e ts --exec \"npm run build && npm run start:prod | pino-pretty\"",
    "test": "jest --passWithNoTests",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:watch": "jest --watch"
  },
  "prettier": "@4s1/eslint-config",
  "dependencies": {
    "@4s1/common-lib": "workspace:*",
    "@4s1/ocpp-lib": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "27.0.1",
    "@types/node": "14.17.15",
    "@4s1/eslint-config": "^3.0.0",
    "eslint": "7.32.0",
    "jest": "27.1.1",
    "nodemon": "2.0.12",
    "pino-pretty": "6.0.0",
    "prettier": "2.3.2",
    "ts-jest": "27.0.5",
    "ts-node": "10.2.1",
    "typescript": "4.3.5"
  },
  "engines": {
    "node": ">=14"
  }
}
