{
  "name": "@yellowgarbagebag/css-lib",
  "version": "1.0.0",
  "private": true,
  "description": "Charging Station Simulator Library",
  "homepage": "https://gitlab.com/YellowGarbageGroup/csms",
  "bugs": {
    "url": "https://gitlab.com/YellowGarbageGroup/csms/issues"
  },
  "repository": {
    "type": "git",
    "url": "git@gitlab.com:yellow-garbage-group/csms.git"
  },
  "license": "MIT",
  "author": "Steffen <YellowGarbageBag@4s1.de>",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "prebuild": "rm -rf dist/",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "preinstall": "npx only-allow pnpm",
    "lbt": "npm run lint && npm run build && npm run test",
    "lint": "eslint --ext .ts src/",
    "lint:fix": "npm run lint -- --fix",
    "start": "node -r ts-node/register src/index.ts | pino-pretty",
    "start:prod": "node dist/index.js",
    "start:watch": "nodemon --watch src/ -e ts --exec \"npm run build && npm run start:prod | pino-pretty\"",
    "test": "jest --passWithNoTests",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:watch": "jest --watch"
  },
  "prettier": "@yellowgarbagegroup/eslint-config",
  "dependencies": {
    "@yellowgarbagebag/common-lib": "workspace:*",
    "@yellowgarbagebag/ocpp-lib": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "26.0.24",
    "@types/node": "^14.17.9",
    "@yellowgarbagegroup/eslint-config": "^2.0.1",
    "eslint": "7.32.0",
    "jest": "26.6.3",
    "nodemon": "2.0.12",
    "pino-pretty": "4.8.0",
    "prettier": "2.3.2",
    "ts-jest": "26.5.6",
    "ts-node": "9.1.1",
    "typescript": "4.3.5"
  },
  "engines": {
    "node": ">=14"
  }
}
